
language: cpp
sudo: false
dist: trusty
compiler:
  - clang
  - gcc
os:
  - linux

before_install:
  - echo "BEFORE INSTALL START"
  - gem install bundler
  - echo "BEFORE INSTALL END"
  
branches:
  only:
  - master

script:
  - cd ${TRAVIS_BUILD_DIR}/tests
  - cmake .
  - make && ./test